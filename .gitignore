node_modules/
dist/
*.zip
.DS_Store

# Chrome for Testing, downloaded on demand by `npm run drive`.
chrome/

# Debug captures from `npm run drive`. The store/ images are committed, since
# they are the published listing assets.
drive-*.png

# Built upload packages — regenerate with `npm run package`.
releases/

# Pages captured from real ATS sites for offline iteration. They are large and
# can contain personal data if saved mid-application, so they stay local.
# Hand-written fixtures (sample-*.html) are committed.
fixtures/captured-*.html

# Personal test data (profile exports, sample resumes). Never committed.
local/
